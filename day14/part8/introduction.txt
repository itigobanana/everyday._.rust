1️⃣配列やタプルは、サイズの決まった箱
しかし、実際のアプリ開発では、動かしてみるまでデータ量がわからないことが多い！
→コレクションを使おう！
コレクション...データが増えても、自動的にサイズを広げて収納してくれるやつ

2️⃣魔法のバッグ3選！
①ベクタ型(Vec<T>) ＝　伸縮自在の横長の棚
データを一列に並べる棚。一番後ろに追加、真ん中に割り込みなど自由にできる

②文字列(String) ＝　文字専用の、伸縮自在の棚

③ハッシュマップ(HashMap) ＝　名前シール付きのロッカー
順番ではなく、名前（キー）と中身（値）をセットで保存する。
